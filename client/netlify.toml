[build.environment]
NODE_VERSION = "22"

[build]
  command = "node inject-env.js && ng build --configuration=production"
  publish = "dist/ludi-front"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200