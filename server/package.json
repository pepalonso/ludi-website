{
  "name": "write-to-db-test",
  "version": "1.0.0",
  "description": "Aixo es una funcio lambda que escriura les dades dels equips del ludi3x3 a la basa de dades",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker build -t my-db ./database && sam build",
    "start": "sam build && docker run -d -p 8001:8001 my-db && sam local start-api"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "luxon": "^3.5.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "globals": "^15.11.0"
  }
}
